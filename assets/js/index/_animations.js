// Generated by CoffeeScript 1.12.7
(function() {
  var Animation;

  Animation = (function() {
    function Animation(element) {
      this.element = element;
    }

    Animation.prototype.move = function(fromX, fromY, toX, toY, duration) {
      var curX, curY, distanceX, distanceY, frames, moveAnimation, seconds, stepX, stepY, steps;
      seconds = duration / 1000;
      frames = 60 * seconds;
      curX = fromX;
      curY = fromY;
      distanceX = toX - fromX;
      distanceY = toY - fromY;
      stepX = distanceX / frames;
      stepY = distanceY / frames;
      steps = 0;
      moveAnimation = (function(_this) {
        return function() {
          _this.element.style.left = (curX += stepX) + "px";
          _this.element.style.top = (curY += stepY) + "px";
          if (++steps < frames) {
            return requestAnimationFrame(moveAnimation);
          }
        };
      })(this);
      return requestAnimationFrame(moveAnimation);
    };

    Animation.prototype.moveSingleFrame = function(toX, toY) {
      var moveAnimation;
      moveAnimation = (function(_this) {
        return function() {
          if (toX !== null) {
            _this.element.style.left = toX + "px";
          }
          if (toY !== null) {
            return _this.element.style.top = toY + "px";
          }
        };
      })(this);
      return requestAnimationFrame(moveAnimation);
    };

    return Animation;

  })();

}).call(this);
